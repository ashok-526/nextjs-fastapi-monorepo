{
  "name": "root",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "typecheck": "pnpm -r run -if-present typecheck"
  }
}

